package engine.board;
import java.util.ArrayList;
import model.Colour;

public class SafeZone {

	Colour colour;
	final ArrayList<Cell> cells;
	
	public SafeZone(Colour colour){
		this.colour = colour;
		// (to be done) creates 4 SAFE cells in the arraylist of cells.
	}
}
